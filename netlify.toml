[build]
  command   = "npm run build"
  publish   = "dist"

# Tell Netlify to serve index.html for every URL so React Router handles routing
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
